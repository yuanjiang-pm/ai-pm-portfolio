# 从零训练一个 63.91M 参数的中文小模型：预训练、微调与本地部署实录

> 基于 [jingyaogong/minimind](https://github.com/jingyaogong/minimind) 从零完成一次完整的 LLM 训练闭环
> 模型规格：8 层 Transformer / hidden size 768 / 词表 6400，架构对齐 Qwen3
> 参数量 63.91M（fp16 精度下权重文件约 128MB——注意**参数量 ≠ 文件体积**，fp16 每参数占 2 字节，63.91M × 2B ≈ 128MB）
> 路径：Mac M1 8GB 本地小样本试跑 → 云端 RTX 3080 Ti 全量训练（约 10 元）→ Ollama 本地部署 → OpenAI 规范 API 化

---

## 一、总体路线

```
选型估算（8GB 能不能跑）
  → 本地小样本试跑（预训练 + SFT，5% 数据）
  → 发现模式坍缩（loss 降、输出烂）
  → 云端全量重跑（10 元对照实验，数据量假设得证）
  → 权重三次格式转换 → Ollama 部署 → OpenAI 兼容 API
```

这条路线的核心设计是**先冒烟再放量**：本地用 1/20 的数据跑通全流程、暴露所有平台坑与训练陷阱，才花第一块钱租 GPU。事实证明这个顺序救了我两次——平台坑（MPS 后端、内存调度）和数据坑（模式坍缩）都在低成本阶段暴露，云端一次到位。

---

## 二、预训练：让模型学会"下一个词预测"

### 任务本质

预训练目标是交叉熵最小化——对文本每个位置，模型输出词表上的概率分布，与真实下一个词比对，等价于最大化真实文本的似然。这是所有 LLM 的底层任务，GPT 和这个 64M 的小模型在这一点上没有任何区别。

### 执行与结果

| | 本地（Mac M1, MPS） | 云端（RTX 3080 Ti 12GB） |
|---|---|---|
| 数据 | 3 万行中文（全量的 ~5%） | 1.2GB 全量 |
| 配置 | batch 8 / seq 256 / 1 epoch | batch 32 / seq 340 / 2 epochs（79,390 步） |
| loss | 8.19 → 3.82 | 7.52 → **1.64** |
| 困惑度 | PPL = exp(3.82) ≈ 45.6 | **PPL ≈ 5.2** |
| 耗时 | 1h55m | 3.2h（约 3.5 元） |

### 读 loss 的两个锚点

读 loss 不能只看"降了多少"，要看**绝对位置**：

- **起点锚**：词表 6400，瞎猜的交叉熵是 ln(6400) ≈ 8.76。起训 loss 8.19 只比瞎猜好一点——这说明模型确实从零开始学，起点健康。
- **质量锚**：PPL 的直观含义是"模型预测下一个词时在几个候选里犹豫"。瞎猜是 6400 个候选；PPL 45.6 = 在 45 个里犹豫，说明语法和语义已入门；PPL 5.2 = 几乎确定——这正是后来事实性问答正确率的来源。

**心得：loss 的意义完全依赖参照系。** 跨词表、跨数据集的 loss 绝对值不可比较，评估训练健康度时先找锚点再下结论。

---

## 三、微调（SFT）：从"续写"到"应答"的换脑

### 为什么预训练完还不会聊天

预训练模型只会**续写**——问它"你是谁"，它接着往下编半句话。因为它学的是"任意文本的下一个词"，不是"问题 → 回答"。SFT 把输入换成对话格式 `{"conversations": [...]}`，并通过 **loss mask 只对 assistant 回复部分计算损失**——不让模型浪费容量学"怎么生成问题"，只学"怎么回答"。对话用 ChatML 模板包裹（`<|im_start|>user ... <|im_end|>`），这些特殊 token 是模型区分角色的唯一依据。

### 两轮执行、两种反馈

| 轮次 | 配置 | loss | 反馈 |
|---|---|---|---|
| 第一轮 | 1 epoch / batch 4 | 4.92 → 2.52 | 换脑成功：输出从"续写形态"变成"应答形态"，但**内容全错**——"形式对了，知识没有" |
| 第二轮 | 官方推荐 2 epoch | 4.18 → 2.31 | **loss 降了，模型反而更差**（见下节） |

云端全量 SFT 最终：1.89 → **1.71**（PPL 5.5），问答内容真实可用。

### 最贵的一课：模式坍缩

第二轮 SFT 后，1.5 万条数据被反复曝光，高频模式（自我介绍、客套话）被强化到压倒一切——模型学会"不管问什么，输出万能自我介绍能让平均 loss 最好看"。这是整个项目里印象最深的失败样本：**指标在涨，产品在死。**

由此提炼两条元规则：

1. **无验证集的 loss 下降 ≠ 模型变好。** 训练 loss 是背书成绩，人工抽检验证输出才是考试。
2. **换数据 > 加轮次。** 多样性是泛化之源，重复是坍缩之路——发现过拟合迹象时，第一反应应该是扩充数据，而不是继续训。

### 云端对照实验的结论

本地与云端唯一的本质差异是数据量（5% vs 100%），其余超参与官方完全一致。结果：瑞利散射从"两版都错"变为"答对"，麻婆豆腐从"乱码"变为"结构化输出"。出发时"64M 装不下知识"的假设被证伪——

> **参数量决定容量上限，但小数据时看到的不是容量上限，而是喂入量下限。**

这个 10 元成本的对照实验，是全程性价比最高的一步。

---

## 四、本地部署：一行权重的三次格式转换

### 转换链路（每环在转换什么）

```
cloud_full_sft_768.pth                ← PyTorch state_dict：纯张量字典，无架构信息
  → convert_model.py                  → Qwen3 格式 safetensors：自描述架构（HF 生态通用）
  → llama.cpp convert_hf_to_gguf.py   → GGUF f16：权重 + tokenizer + 元数据单文件打包
  → ollama create                     → Ollama 模型：GGUF + Modelfile 行为配置层
```

本质：**张量自始至终不变，变的是"张量旁边携带什么信息"。** 训练生态和推理生态是两个世界，中间的格式转换不可跳过。

### 最大的部署坑：BPE pre-tokenizer

MiniMind 自训 tokenizer 的预分词规则不被 llama.cpp 自动识别。BPE 分词分两步：先按正则切"预分词块"（数字不跨块、中英文分开），再逐块做 BPE 合并——预分词不对齐，同一文本会切成不同的块序列，模型输出直接乱码。解法：给 llama.cpp 的 `conversion/base.py` 打补丁，让 tokenizer 回落到 **qwen2** 预分词类型（先试 gpt2，被 Ollama 运行时白名单拒绝）。

### Modelfile：推理模板必须与训练模板逐字节对齐

```
<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
<think>

</think>

{{ .Response }}<|im_end|>
```

注意**空思考块**：SFT 数据带 `<think>\n\n</think>\n\n` 前缀，推理模板若与之不一致，模型等于在"从没见过的上文"后面续写，质量立刻劣化。

**心得：部署质量出问题，第一排查项永远是模板对齐，而不是模型本身。** 一个空行的差异就够让输出崩掉。

### 部署验收

- `ollama run` 问答连贯，事实问答（瑞利散射、机器学习分类）正确
- 生成速度 38–49 tokens/s（M1 统一内存，fp16）
- OpenAI 兼容端点 `localhost:11434/v1` 直接可用，标准 SDK 零改造接入

---

## 五、API 化：让任意 OpenAI 客户端接入

### 架构：代理只补一道门

Ollama 本身就暴露 OpenAI 兼容端点（`localhost:11434/v1`），但不校验任何密钥。所以方案是极简的一层代理：

```
客户端（Trae / WorkBuddy / OpenAI SDK）
  → FastAPI 代理（:8787）：Bearer 鉴权 + 请求整形 + 输出防护
    → Ollama 原生 OpenAI 端点（:11434/v1）
```

鉴权用 Bearer 密钥（热更新密钥文件，新增/吊销即时生效，401 返回 OpenAI 标准 error 格式）；服务用 launchd 常驻，开机自启、崩溃自愈。

### 接入真实客户端后，十轮排障实录

接入 IDE 类 Agent 客户端（Trae、WorkBuddy）后，真实世界立刻上了一课：**OpenAI 规范只约定接口形状，客户端在消息体里塞的"私货"才是适配层的真正战场。** 以下每轮都是"现象 → 日志实锤 → 根因 → 修复"：

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 流式输出中文乱码 | `aiter_bytes` 把跨 chunk 的 UTF-8 多字节字符劈开 | 改 `aiter_text` 按字符解码 |
| 2 | 客户端回答为空 | Agent 系统提示词 6028 token > num_ctx 4096，Ollama 打回 400 | num_ctx 提至 8192 |
| 3 | 提示词超长后胡说 | 64M 模型消化不了 >5000 字符的提示词（实测边界） | 代理请求瘦身：剥 tools、替换巨型 system、只留真问题 |
| 4 | 回复变英文字面量 | 新客户端 content 是分段列表 `[{"type":"text",...}]`，`str()` 直转污染 | 解析器兼容两种 content 形态 |
| 5 | 答非所问 | Agent 工作流指令伪装成最后一条 user 消息，瘦身误保留、真问题被丢弃 | 注入指令特征识别 + 真问题提取 |
| 6 | 问什么都是自我介绍 | few-shot 示例在 temp 0 下形成强锚点，相似问题全被劫持 | 移除 few-shot，身份话术改由代理层正则拦截直返（100% 逐字） |
| 7 | Trae Agent 模式失效 | 真问题嵌在 `<user_input>` 标签里，整段模板被当原文保留 | 正则提取标签内文本 |
| 8 | 换 WorkBuddy 又失效 | WorkBuddy 用的是 `<user_query>` 标签——每家客户端标签都不同 | 提取正则兼容多标签 |
| 9 | 复读死循环刷屏 56 万字符 | temp 0 贪心解码的固有点头循环 + 无 max_tokens 刹车 | max_tokens 熔断（512/1024）+ 复读修剪 |
| 10 | 修剪对 WorkBuddy 无效 | 它走 stream=true，透传 SSE 发出去就收不回 | 流式改内部非流式 → 修剪 → 模拟 SSE 分块下发 |

### 温度策略的 A/B 裁决

熔断只是止血，复读根源在贪心解码。做了一组对照实验（同一批问题 × temp 0 vs 0.7 × 各 3 轮，行级重复率打分）：

| 问题 | temp 0（贪心） | temp 0.7（采样） |
|---|---|---|
| 下雨要打伞吗 | **95% / 95% / 95%** | **0% / 0% / 0%** |
| 其他问题 | 0% | 0% |

贪心解码下"下雨要打伞吗"是**确定性循环**——同一个概率局部陷阱，每次必掉进去。而放开温度之所以安全，是因为第 6 轮已把身份话术的锁定职责移交给了代理层：**模型内做不准的事，就挪到系统层做。** 最终配置 temp 0.7 + 熔断 + 修剪三层防护，重复率归零。

### 一个值得复盘的现象：为什么 API 端比 CLI 表现好

同样的模型，IDE 客户端里的回答明显好于 `ollama run` 命令行。归因到三个变量：

1. **输入卫生**：代理把几万字符的脏模板瘦成一条干净短问题；CLI 把多轮历史原样累积——实测系统提示词超 5000 字符小模型就开始胡说
2. **训练分布对齐**：SFT 学的是短问短答的单轮对话，瘦身后喂给它的恰好就是这种形态，模型处在最擅长的作答状态
3. **错误容忍度**：API 管线有拦截、熔断、修剪三道保险，异常输出在到达用户眼前已被修掉

> **模型的表现 = 模型 × 服务配置 × 输入卫生。** 同一个权重，任何一条拉胯都表现为"模型变笨了"。

---

## 六、关键数字总览

| 指标 | 本地（M1 8GB） | 云端（3080 Ti） |
|---|---|---|
| 预训练 loss / PPL | 8.19 → 3.82 / 45.6 | 7.52 → **1.64 / 5.2** |
| SFT loss / PPL | 4.92 → 2.52（2ep 2.31）/ 12.4 | 1.89 → **1.71 / 5.5** |
| 总耗时 | ~5.5h（5% 数据） | ~9.3h / **约 10 元** |
| 事实性问答 | 全错（有形式无知识） | 瑞利散射等答对 |

---

## 七、心得体会

1. **训练 loss 是背书成绩，验证输出才是考试。** 模式坍缩的教训：盯着 loss 曲线不看实际输出，会被 2 epoch 的"进步"骗。任何训练阶段都要留一个人工抽检的口子。
2. **数据量是第一杠杆。** 10 元的对照实验证明瓶颈从来不在模型——"模型太小"往往是"数据太少"的误诊。先确认喂饱了，再怀疑容量。
3. **训练与推理是两个生态。** 权重转换、pre-tokenizer 兼容、模板对齐——部署环节的坑没有一个在训练时埋的雷区里，且每一个都能独立让模型"变笨"。
4. **先冒烟再放量。** 用 5% 数据和免费的本机算力把全流程踩一遍，所有昂贵的错误都在便宜的阶段发生。
5. **小模型是理解 LLM 的最佳教具。** 64M 的体量让训练能在几小时内完成、能在 8GB 的笔记本上跑、失败能在一眼内归因——把 GPT 的所有机制（预训练、SFT、模式坍缩、格式转换）都压缩进一个可亲手把玩的尺度。
6. **适配层是 LLM 工程化的隐藏主战场。** 十轮排障没有一轮出在模型或训练上——全在"客户端方言"与"模型能力边界"之间：UTF-8 劈字符、分段 content、注入模板、多标签提取、复读熔断。OpenAI 规范只是接口形状的约定，把每家客户端的私货翻译成小模型能懂的话，才是让模型"显得聪明"的真正工程。
7. **确定性锚点是双刃剑，模型内做不准的事就挪到系统层做。** few-shot 能锁身份话术（5/5 逐字），但会劫持相似问题；最终方案是代理层拦截身份问题直返话术、模型专注正常问答——职责分离后，温度也能放心放开。

---

## 八、致谢

- [jingyaogong/minimind](https://github.com/jingyaogong/minimind)：训练代码、数据处理与超参均基于该项目，Apache-2.0
- [llama.cpp](https://github.com/ggml-org/llama.cpp) 与 [Ollama](https://ollama.com)：本地推理与部署基础设施
